<!DOCTYPE html>
<html>
<head>
  <title>Movie Overview</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f4;
      margin: 0;
      padding: 0;
    }
    h1 {
      background-color: #333;
      color: #fff;
      text-align: center;
      padding: 10px 0;
      margin: 0;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 20px;
    }
    th, td {
      padding: 10px;
      text-align: left;
    }
    th {
      background-color: #333;
      color: #fff;
      font-weight: bold;
    }
    tr:nth-child(even) {
      background-color: #f2f2f2;
    }
    tr:hover {
      background-color: #ddd;
    }
    .center-container {
      display: flex;
      justify-content: center;
      align-items: center;
    }
    .search-form {
      margin: 20px 0;
      text-align: center;
    }
    .search-form__form {
      display: inline-block;
      text-align: left;
    }
    .search-form__label {
      font-size: 14px;
      margin-right: 10px;
    }
    .search-form__input {
      padding: 6px 10px;
      border: 1px solid #ccc;
      border-radius: 4px;
      font-size: 14px;
      max-width: 300px;
    }
    .search-form__submit {
      padding: 6px 12px;
      background-color: #333;
      color: #fff;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      font-size: 14px;
    }
  </style>
</head>
<body>
  <div class="center-container">
    <div>
      <h1>Movie Overview</h1>
      <%= render partial: 'search_form' %>
    </div>
  </div>

  <table>
    <thead>
      <tr>
        <th>Title</th>
        <th>Description</th>
        <th>Year</th>
        <th>Director</th>
        <th>Actor</th>
        <th>Filming Location</th>
        <th>Country</th>
        <th>Ratings</th>
        <th>Added on</th>
      </tr>
    </thead>
    <tbody>
      <% @movies.each do |movie| %>
        <tr>
          <td><%= movie.title %></td>
          <td><%= movie.description %></td>
          <td><%= movie.year %></td>
          <td><%= movie.director %></td>
          <td><%= movie.actors.to_sentence %></td>
          <td><%= movie.filming_location %></td>
          <td><%= movie.country %></td>
          <td><%= calculate_average_rating(movie) %></td></td>
          <td><%= time_ago_in_words(movie.created_at) %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</body>
</html>
